version: '3.8'

services:
  postgres:
    image: postgres:15
    container_name: pwoffice-postgres
    restart: always
    environment:
      - POSTGRES_DB=${PGDATABASE:-pwoffice}
      - POSTGRES_USER=${PGUSER:-pwadmin}
      - POSTGRES_PASSWORD=${PGPASSWORD}
    volumes:
      - pgdata:/var/lib/postgresql/data
    ports:
      - "5432:5432"
    networks:
      - pwoffice-network

  docserver:
    image: pwoffice-server:final
    container_name: pwoffice-docserver
    restart: always
    environment:
      - JWT_ENABLED=true
      - JWT_SECRET=${JWT_SECRET}
      - JWT_HEADER=Authorization
      - JWT_IN_BODY=true
      - ALLOW_PRIVATE_IP_ADDRESS=true
    volumes:
      - docserver_data:/var/www/onlyoffice/Data
      - docserver_log:/var/log/onlyoffice
    expose:
      - "80"
    networks:
      - pwoffice-network

  webapp:
    build:
      context: ./pwoffice-webapp
      dockerfile: Dockerfile
    container_name: pwoffice-webapp
    restart: always
    depends_on:
      - postgres
      - docserver
    environment:
      - PORT=3000
      - NODE_ENV=production
      - JWT_SECRET=${JWT_SECRET}
      - DOCUMENT_SERVER_PUBLIC_URL=${DOCUMENT_SERVER_PUBLIC_URL:-http://localhost}
      - DOCUMENT_SERVER_INTERNAL_URL=http://docserver
      - WEBAPP_PUBLIC_URL=${WEBAPP_PUBLIC_URL:-http://localhost}
      - WEBAPP_INTERNAL_URL=http://webapp:3000
      - PGHOST=postgres
      - PGPORT=5432
      - PGUSER=${PGUSER:-pwadmin}
      - PGPASSWORD=${PGPASSWORD}
      - PGDATABASE=${PGDATABASE:-pwoffice}
      - GROQ_API_KEY=${GROQ_API_KEY}
      - SMTP_HOST=${SMTP_HOST:-smtp.gmail.com}
      - SMTP_PORT=${SMTP_PORT:-587}
      - SMTP_USER=${SMTP_USER}
      - SMTP_PASS=${SMTP_PASS}
      - EMAIL_FROM=${EMAIL_FROM}
    volumes:
      - ./pwoffice-webapp/server.js:/app/server.js
      - ./pwoffice-webapp/routes:/app/routes
      - ./pwoffice-webapp/views:/app/views
      - ./pwoffice-webapp/public:/app/public
      - ./pwoffice-webapp/utils:/app/utils
      - webapp-data:/app/data
      - webapp-storage:/app/storage
    ports:
      - "3010:3000"
    networks:
      - pwoffice-network

  nginx:
    image: nginx:alpine
    container_name: pwoffice-nginx
    restart: always
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
      - ./certs:/etc/letsencrypt:ro
      - certbot-web:/var/www/certbot
    depends_on:
      - webapp
      - docserver
    networks:
      - pwoffice-network

  certbot:
    image: certbot/certbot
    container_name: pwoffice-certbot
    volumes:
      - ./certs:/etc/letsencrypt
      - certbot-web:/var/www/certbot
    entrypoint: "/bin/sh -c 'trap exit 0; while :; do certbot renew; sleep 12h & wait $${!}; done;'"
    networks:
      - pwoffice-network

volumes:
  pgdata:
  docserver_data:
  docserver_log:
  webapp-data:
  webapp-storage:
  certbot-web:

networks:
  pwoffice-network:
    driver: bridge
